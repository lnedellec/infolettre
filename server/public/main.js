const editor = grapesjs.init({
    container : "#editor",
    fromElement: true,
    width: "auto",
    storageManager: false,
    plugins:["gjs-preset-newsletter"],
    plugins0pts:{
        "gjs-preset-newsletter": {},
    }
});